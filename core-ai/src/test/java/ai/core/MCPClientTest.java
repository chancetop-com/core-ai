package ai.core;

import org.junit.jupiter.api.Test;

/**
 * @author stephen
 */
class MCPClientTest {
    @Test
    void test() {
//        var service = new MCPClientService(new MCPServerConfig("localhost", 8080));
//        service.callTool("git_log", "{\"repo_path\": \".\", \"max_count\": 10}", null);
//        var rst = service.listTools();
//        service.close();
//        logger.info(rst.stream().map(v -> Strings.format("<Tool>[{}]:{}", v.getName(), v.getInputSchema().toString())).collect(Collectors.joining("\n")));
    }
}
